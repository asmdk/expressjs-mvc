div(id='navbar' class='navbar navbar-inverse')
    ul(class='nav navbar-nav')
        each menuItem in mainMenu
            li(class='menu-'+menuItem.id)
                a(href=menuItem.link) #{menuItem.title}